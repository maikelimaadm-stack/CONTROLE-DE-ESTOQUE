/**
 * O detector de rota do ID Global vive no DOMÍNIO (deriva do catálogo e não depende de React). Este
 * reexport existe só para a UI continuar importando de `@/lib`, como todo o resto do app.
 */
export { entidadeDaRota, ROTAS_CANONICAS_ID_GLOBAL } from "@agro/domain";
